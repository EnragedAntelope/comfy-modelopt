# comfy-modelopt - Core Dependencies
#
# This package runs as a ComfyUI custom node. PyTorch is expected to be
# provided by the ComfyUI environment — do NOT install a conflicting one.

# NVIDIA ModelOpt (calibration engine) — builds C++ extensions at install time
nvidia-modelopt[all]>=0.43.0

# Model handling
safetensors>=0.3.0
accelerate>=0.20.0

# Hugging Face libraries
transformers>=4.30.0
diffusers>=0.20.0

# Optional
onnx>=1.14.0
onnxruntime>=1.15.0

# ─── Platform Notes ──────────────────────────────────────────
# Windows: pip install -r requirements-windows.txt
#   (includes triton-windows, ninja, onnxruntime-gpu)
#
# ─── Known setuptools conflict (cosmetic only) ──────────────
# nvidia-modelopt requires setuptools>=80 (build time).
# PyTorch may pin setuptools<82 (pip-installed dependency).
#
# If pip warns about this, ignore it — modelopt works fine
# at runtime regardless. The conflict does NOT affect ComfyUI
# or any modelopt functionality.
